{
  "name": "hvcs-webapp",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "deploy:functions": "cd firebase && npx firebase-tools deploy --only functions",
    "deploy:firestore": "cd firebase && npx firebase-tools deploy --only firestore",
    "deploy:storage": "cd firebase && npx firebase-tools deploy --only storage"
  }
}
