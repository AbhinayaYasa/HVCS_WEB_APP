{
  "name": "hvcs-webapp",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "install:frontend": "npm install --prefix frontend",
    "deploy:firestore": "cd firebase && npx firebase-tools deploy --only firestore"
  },
  "description": "Hudson Valley Concierge Services — Web application",
  "dependencies": {
    "firebase": "^12.8.0"
  }
}
